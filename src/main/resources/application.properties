spring.application.name=baleshop
server.port=${SERVER_PORT:8080}
spring.datasource.url=jdbc:mysql://localhost:3306/bale_shop
spring.datasource.username=balesuser
spring.datasource.password=@Yenkasa01@$$

jakarta.persistence.jdbc.user=balesuser
jakarta.persistence.jdbc.password=@Yenkasa01@$$

jakarta.persistence.jdbc.url=jdbc:mysql://localhost:3306/bale_shop

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB
cloudinary.cloud-name=dame7a9rv
cloudinary.api-key=267827217416528
cloudinary.api-secret=uA7CtSuciHpwW-VllhzLyT5sdfA

app.frontend-base-url=${FRONTEND_URL:https://www.yenkasa.xyz}/store
app.paystack.secret-key=${PAYSTACK_SECRET_KEY:}
app.paystack.public-key=${PAYSTACK_PUBLIC_KEY:}
app.support.email=${SUPPORT_EMAIL:support@store.yenkas.xyz}
app.uber.client-id=${UBER_CLIENT_ID:}
app.uber.client-secret=${UBER_CLIENT_SECRET:}
app.uber.scope=${UBER_SCOPE:eats.deliveries}
app.uber.token-url=${UBER_TOKEN_URL:https://auth.uber.com/oauth/v2/token}
app.uber.api-base-url=${UBER_API_BASE_URL:https://api.uber.com/v1}
app.uber.customer-id=${UBER_CUSTOMER_ID:}
app.uber.pickup-name=${UBER_PICKUP_NAME:Yenkasa Store}
app.uber.pickup-phone=${UBER_PICKUP_PHONE:}
app.uber.pickup-address=${UBER_PICKUP_ADDRESS:}
app.uber.webhook-signing-key=${UBER_WEBHOOK_SIGNING_KEY:}
app.mailer.node-binary=${NODE_BINARY:node}
app.mailer.script-path=${MAILER_SCRIPT_PATH:scripts/send-email.js}
app.mailer.resend-api-url=${RESEND_API_URL:https://api.resend.com/emails}
app.mailer.zeptomail-api-url=${ZEPTOMAIL_API_URL:https://api.zeptomail.com/v1.1/email}
app.super-admin.email=ofosumenyabrightkofi@gmail.com
app.super-admin.password=@Yenkasa01@$$
app.super-admin.name=Bright Kofi Ofosu Menya

# SMTP / transactional email
spring.mail.host=${SMTP_HOST:}
spring.mail.port=${SMTP_PORT:587}
spring.mail.username=${EMAIL_USER:}
spring.mail.password=${EMAIL_PASS:}
spring.mail.from=${EMAIL_FROM:${spring.mail.username:}}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.enable=${SMTP_SECURE:false}
spring.mail.properties.mail.smtp.ssl.trust=${SMTP_HOST:}
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000
